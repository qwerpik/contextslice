// Package name deliberately differs from the directory base name: the
// qualifier must come from this clause, not the path tail.
package helpersvc

// H is called through the package name.
func H() {}
